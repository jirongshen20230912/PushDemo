<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">
        <!--角度-->
        <corners android:radius="5dp"/>
        <!--渐变色-->
        <!--<gradient
            android:angle="180"
            android:endColor="@color/main_shape_item1"
            android:startColor="@color/main_shape_item1" />-->
        <!-- 填充色 -->
        <solid  android:color="@color/shape_login"/>
        <!-- 描边 设置线宽及颜色 -->
        <!--<stroke android:color="@color/goto1"
            android:width="1dp"/>-->
</shape>